services:
    restate:
        image: docker.restate.dev/restatedev/restate:latest
        ports:
            - "8080:8080" # Ingress — send requests here
            - "9070:9070" # Admin API + Web UI
